# API Map — Agent Sandbox v1alpha1 to v1beta1

| old_api | new_api | kind | action | before | after | notes | source_section |
|---|---|---|---|---|---|---|---|
| agents.x-k8s.io/v1alpha1 (Sandbox) | agents.x-k8s.io/v1beta1 (Sandbox) | class | replace | `apiVersion: agents.x-k8s.io/v1alpha1` | `apiVersion: agents.x-k8s.io/v1beta1` | Conversion webhook handles automatically during storage rewrite | What changes between versions |
| agents.x-k8s.io/v1alpha1 (SandboxClaim) | agents.x-k8s.io/v1beta1 (SandboxClaim) | class | replace | `apiVersion: agents.x-k8s.io/v1alpha1` | `apiVersion: agents.x-k8s.io/v1beta1` | Not field-compatible; requires bootstrap phase for cold-start claims | What changes between versions |
| agents.x-k8s.io/v1alpha1 (SandboxTemplate) | agents.x-k8s.io/v1beta1 (SandboxTemplate) | class | replace | `apiVersion: agents.x-k8s.io/v1alpha1` | `apiVersion: agents.x-k8s.io/v1beta1` | Structurally identical but needs storage rewrite | What changes between versions |
| agents.x-k8s.io/v1alpha1 (SandboxWarmPool) | agents.x-k8s.io/v1beta1 (SandboxWarmPool) | class | replace | `apiVersion: agents.x-k8s.io/v1alpha1` | `apiVersion: agents.x-k8s.io/v1beta1` | Structurally identical but needs storage rewrite | What changes between versions |
| spec.sandboxTemplateRef + spec.warmpool (SandboxClaim) | spec.warmPoolRef.name (SandboxClaim) | field | replace | `spec.sandboxTemplateRef: my-template`; `spec.warmpool: "default"` | `spec.warmPoolRef.name: shadow-pool-my-template` | Conversion webhook handles rewriting; cold-start claims need shadow pools pre-created via bootstrap phase | SandboxClaim is not field-compatible |
| spec.warmpool: "none" (SandboxClaim) | spec.warmPoolRef.name: shadow-pool-<template> (SandboxClaim) | field | replace | `spec.warmpool: "none"` | `spec.warmPoolRef.name: shadow-pool-<template-name>` | Cold-start claim; bootstrap creates shadow pool per (namespace, template) | SandboxClaim is not field-compatible |
| spec.warmpool: "" (SandboxClaim) | spec.warmPoolRef.name (SandboxClaim) | field | replace | `spec.warmpool: ""` | `spec.warmPoolRef.name: <derived-pool-name>` | Empty string treated as default; warm-started derives pool name via stripRandomSuffix | SandboxClaim is not field-compatible |
| spec.warmpool: "default" (SandboxClaim) | spec.warmPoolRef.name (SandboxClaim) | field | replace | `spec.warmpool: "default"` | `spec.warmPoolRef.name: <derived-pool-name>` | Warm-started: derives via stripRandomSuffix; cold-start: redirects to shadow-pool-<template> | SandboxClaim is not field-compatible |
| spec.warmpool: <specific-pool> (SandboxClaim) | spec.warmPoolRef.name: <specific-pool> (SandboxClaim) | field | replace | `spec.warmpool: "my-pool"` | `spec.warmPoolRef.name: "my-pool"` | Used verbatim; if pool doesn't exist, claim points at missing pool — operator must create it | SandboxClaim is not field-compatible |
| spec.replicas: 0 (Sandbox) | spec.operatingMode: Suspended (Sandbox) | field | replace | `spec.replicas: 0` | `spec.operatingMode: Suspended` | Handled automatically by conversion webhook | Sandbox.spec.replicas becomes Sandbox.spec.operatingMode |
| spec.replicas: 1 (Sandbox) | spec.operatingMode: Running (Sandbox) | field | replace | `spec.replicas: 1` | `spec.operatingMode: Running` | Also applies when replicas is unset (default) | Sandbox.spec.replicas becomes Sandbox.spec.operatingMode |
