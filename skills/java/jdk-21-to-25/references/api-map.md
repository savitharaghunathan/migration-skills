# API Map — JDK 21 to 25

| old_api | new_api | kind | action | before | after | notes | source_section |
|---|---|---|---|---|---|---|---|
| sun.misc.Unsafe#shouldBeInitialized(Class) | java.lang.invoke.MethodHandles.Lookup#ensureInitialized(Class) | method | remove | `Unsafe.getUnsafe().shouldBeInitialized(Foo.class)` | `MethodHandles.lookup().ensureInitialized(Foo.class)` | Removed in JDK 22 | Removed APIs (JDK 22) |
| sun.misc.Unsafe#ensureClassInitialized(Class) | java.lang.invoke.MethodHandles.Lookup#ensureInitialized(Class) | method | replace | `Unsafe.getUnsafe().ensureClassInitialized(Foo.class)` | `MethodHandles.lookup().ensureInitialized(Foo.class)` | Removed in JDK 22 | Removed APIs (JDK 22) |
| java.lang.Thread#countStackFrames() | removed | method | remove | `thread.countStackFrames()` | (remove — method was already deprecated and non-functional) | Removed in JDK 22 | Removed APIs (JDK 22) |
| sun.misc.Unsafe#park(boolean, long) | java.util.concurrent.locks.LockSupport#park() | method | replace | `Unsafe.getUnsafe().park(false, 0L)` | `LockSupport.park()` | Deprecated for removal in JDK 22 | Deprecated for Removal (JDK 22) |
| sun.misc.Unsafe#unpark(Thread) | java.util.concurrent.locks.LockSupport#unpark(Thread) | method | replace | `Unsafe.getUnsafe().unpark(thread)` | `LockSupport.unpark(thread)` | Deprecated for removal in JDK 22 | Deprecated for Removal (JDK 22) |
| sun.misc.Unsafe#getLoadAverage(double[], int) | java.lang.management.OperatingSystemMXBean#getSystemLoadAverage() | method | replace | `Unsafe.getUnsafe().getLoadAverage(loadavg, 1)` | `ManagementFactory.getOperatingSystemMXBean().getSystemLoadAverage()` | Deprecated for removal in JDK 22 | Deprecated for Removal (JDK 22) |
| sun.misc.Unsafe#loadFence() | java.lang.invoke.VarHandle#loadLoadFence() | method | replace | `Unsafe.getUnsafe().loadFence()` | `VarHandle.loadLoadFence()` | Deprecated for removal in JDK 22; use VarHandle fence methods | Deprecated for Removal (JDK 22) |
| sun.misc.Unsafe#storeFence() | java.lang.invoke.VarHandle#storeStoreFence() | method | replace | `Unsafe.getUnsafe().storeFence()` | `VarHandle.storeStoreFence()` | Deprecated for removal in JDK 22; use VarHandle fence methods | Deprecated for Removal (JDK 22) |
| sun.misc.Unsafe#fullFence() | java.lang.invoke.VarHandle#fullFence() | method | replace | `Unsafe.getUnsafe().fullFence()` | `VarHandle.fullFence()` | Deprecated for removal in JDK 22; use VarHandle fence methods | Deprecated for Removal (JDK 22) |
| java.lang.Thread#suspend() | removed | method | remove | `thread.suspend()` | (remove — use cooperative signaling with `volatile` flags or `LockSupport`) | Removed in JDK 23 | Removed APIs (JDK 23) |
| java.lang.Thread#resume() | removed | method | remove | `thread.resume()` | (remove — use cooperative signaling with `volatile` flags or `LockSupport`) | Removed in JDK 23 | Removed APIs (JDK 23) |
| java.lang.ThreadGroup#suspend() | removed | method | remove | `threadGroup.suspend()` | (remove — use cooperative signaling per thread) | Removed in JDK 23 | Removed APIs (JDK 23) |
| java.lang.ThreadGroup#resume() | removed | method | remove | `threadGroup.resume()` | (remove — use cooperative signaling per thread) | Removed in JDK 23 | Removed APIs (JDK 23) |
| java.lang.ThreadGroup#stop() | removed | method | remove | `threadGroup.stop()` | (remove — use cooperative shutdown per thread) | Removed in JDK 23 | Removed APIs (JDK 23) |
| javax.naming.Context#APPLET | removed | field | remove | `Context.APPLET` | (remove — applet-based JNDI is obsolete) | Constant removed in JDK 24 | Removed APIs (JDK 24) |
| java.util.zip.ZipError | removed | class | remove | `catch (ZipError e)` | `catch (InternalError e)` | Deprecated for removal in JDK 24 | Deprecated for Removal (JDK 24) |
| java.lang.SecurityManager | removed | class | remove | `System.setSecurityManager(new SecurityManager())` | (remove — Security Manager permanently disabled in JDK 24, JEP 486) | API retained but all methods are no-ops; will be removed in future release | Permanently Disable the Security Manager (JDK 24) |
| java.security.Policy | removed | class | remove | `Policy.getPolicy()` | (remove — Security Manager permanently disabled) | Part of Security Manager removal | Permanently Disable the Security Manager (JDK 24) |
| java.security.Permission | removed | class | remove | `new FilePermission("/tmp/*", "read")` | (remove — Security Manager permanently disabled) | Various Permission subclasses deprecated for removal in JDK 25 | Deprecated for Removal (JDK 25) |
| sun.misc.Unsafe#getInt(Object, long) | java.lang.foreign.MemorySegment | method | replace | `Unsafe.getUnsafe().getInt(obj, offset)` | Use `MemorySegment` and `VarHandle` APIs | All memory-access methods deprecated for removal (JEP 471, JDK 23); runtime warning in JDK 24 (JEP 498) | Deprecated for Removal (JDK 23) |
| sun.misc.Unsafe#putInt(Object, long, int) | java.lang.foreign.MemorySegment | method | replace | `Unsafe.getUnsafe().putInt(obj, offset, val)` | Use `MemorySegment` and `VarHandle` APIs | All memory-access methods deprecated for removal (JEP 471, JDK 23) | Deprecated for Removal (JDK 23) |
| sun.misc.Unsafe#getObject(Object, long) | java.lang.foreign.MemorySegment | method | replace | `Unsafe.getUnsafe().getObject(obj, offset)` | Use `VarHandle` API | All memory-access methods deprecated for removal (JEP 471, JDK 23) | Deprecated for Removal (JDK 23) |
| sun.misc.Unsafe#putObject(Object, long, Object) | java.lang.foreign.MemorySegment | method | replace | `Unsafe.getUnsafe().putObject(obj, offset, val)` | Use `VarHandle` API | All memory-access methods deprecated for removal (JEP 471, JDK 23) | Deprecated for Removal (JDK 23) |
| sun.misc.Unsafe#compareAndSwapInt(Object, long, int, int) | java.lang.invoke.VarHandle#compareAndSet(Object...) | method | replace | `Unsafe.getUnsafe().compareAndSwapInt(obj, offset, expected, newVal)` | Use `VarHandle.compareAndSet()` | All memory-access methods deprecated for removal (JEP 471, JDK 23) | Deprecated for Removal (JDK 23) |
| sun.misc.Unsafe#compareAndSwapLong(Object, long, long, long) | java.lang.invoke.VarHandle#compareAndSet(Object...) | method | replace | `Unsafe.getUnsafe().compareAndSwapLong(obj, offset, expected, newVal)` | Use `VarHandle.compareAndSet()` | All memory-access methods deprecated for removal (JEP 471, JDK 23) | Deprecated for Removal (JDK 23) |
| sun.misc.Unsafe#compareAndSwapObject(Object, long, Object, Object) | java.lang.invoke.VarHandle#compareAndSet(Object...) | method | replace | `Unsafe.getUnsafe().compareAndSwapObject(obj, offset, expected, newVal)` | Use `VarHandle.compareAndSet()` | All memory-access methods deprecated for removal (JEP 471, JDK 23) | Deprecated for Removal (JDK 23) |
| sun.misc.Unsafe#allocateMemory(long) | java.lang.foreign.Arena#allocate(long) | method | replace | `Unsafe.getUnsafe().allocateMemory(size)` | `Arena.ofConfined().allocate(size)` | All memory-access methods deprecated for removal (JEP 471, JDK 23) | Deprecated for Removal (JDK 23) |
| sun.misc.Unsafe#freeMemory(long) | java.lang.foreign.Arena#close() | method | replace | `Unsafe.getUnsafe().freeMemory(address)` | `arena.close()` | All memory-access methods deprecated for removal (JEP 471, JDK 23) | Deprecated for Removal (JDK 23) |
| java.net.Socket(java.net.DatagramSocketImpl) | removed | method | remove | `new Socket(datagramSocketImpl)` | (remove — use `DatagramSocket` directly for datagram sockets) | Socket constructors can no longer create datagram sockets; removed in JDK 25 | Removed APIs (JDK 25) |
| java.beans.beancontext | removed | package | remove | `import java.beans.beancontext.*` | (remove — package deprecated for removal in JDK 23) | Entire package deprecated | Deprecated for Removal (JDK 23) |
| java.io.IO | java.lang.IO | class | move_package | `import java.io.IO; IO.println("Hello");` | `IO.println("Hello, world!")` | IO class moved from java.io to java.lang in JDK 25; now implicitly imported | Compact Source Files (JDK 25) |
